{
  "build": {
    "env": {
      "PYTHON_VERSION": "3.9"
    }
  },
  "buildCommand": "pip install -r requirements.txt && mkdocs build -d public",
  "outputDirectory": "public",
  "installCommand": "echo 'Skipping npm install'"
}
